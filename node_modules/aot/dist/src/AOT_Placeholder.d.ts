declare type aot_flag = {
    type: "Then";
    data: {
        then_fun_name: string;
        prop_name: string;
    };
} | {
    type: "Wait";
    data: {
        condition_promise_fun_name: string;
        skip_if_false: boolean;
        prop_name: string;
    };
};
export declare class AOT_Placeholder {
    static GetAOTFlags(target: object): aot_flag[];
    static GetAndSetAOTFlags(target: object): aot_flag[];
    static Then(then_fun_name: string): (target: object, name: string, des: PropertyDescriptor) => PropertyDescriptor;
    static Wait(condition_promise_fun_name: string, skip_if_false?: boolean): (target: object, name: string, des: PropertyDescriptor) => PropertyDescriptor;
}
export {};
